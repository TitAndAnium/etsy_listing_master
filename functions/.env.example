# Firebase Functions – example environment (no secrets)
# Copy this file to `functions/.env` locally and fill in real values.
# NEVER commit real secrets.

# === OpenAI ===
OPENAI_API_KEY=YOUR_OPENAI_API_KEY
# Optional override if you self-host or proxy:
# OPENAI_BASE_URL=https://api.openai.com/v1
# Model hint used by prompts/logging (matches current logs):
OPENAI_MODEL=gpt-4o-2024-08-06

# === App defaults (safe for local/CI) ===
PROJECT_ID=etsy-ai-hacker
TEST_UID=testuser123
RUN_ID=manual-run

# === Emulator hints (optional) ===
# FIRESTORE_EMULATOR_HOST=localhost:8080
# FUNCTIONS_EMULATOR_HOST=localhost:5001
