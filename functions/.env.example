# Firebase Functions – example environment (no secrets)
# Copy this file to `functions/.env` locally and fill in real values.
# NEVER commit real secrets.
#
# === PRODUCTION SECRETS ===
# These should be set via Firebase Secrets in production:
#   firebase functions:secrets:set OPENAI_API_KEY
#   firebase functions:secrets:set STRIPE_SECRET
#   firebase functions:secrets:set STRIPE_WEBHOOK_SECRET
#   firebase functions:secrets:set SLACK_WEBHOOK_URL

# === OpenAI ===
OPENAI_API_KEY=sk-proj-YOUR_OPENAI_API_KEY_HERE
# Optional override if you self-host or proxy:
# OPENAI_BASE_URL=https://api.openai.com/v1
# Model hint used by prompts/logging:
OPENAI_MODEL=gpt-4o-2024-08-06

# === Stripe ===
STRIPE_SECRET=sk_test_YOUR_STRIPE_SECRET_KEY
STRIPE_WEBHOOK_SECRET=whsec_YOUR_STRIPE_WEBHOOK_SECRET
APP_BASE_URL=https://us-central1-etsy-ai-hacker.cloudfunctions.net

# === Slack Alerts ===
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK
ALERT_LATENCY_MS=8000
DAILY_BUDGET_USD=25
BUDGET_HARD_STOP=1
DAILY_CREDITS=500

# === CORS Configuration ===
# CORS whitelist voor productie (comma-separated)
# Development: laat leeg of zet op localhost origins
# Production: https://etsy-ai-hacker.web.app,https://etsy-ai-hacker.firebaseapp.com
ALLOWED_ORIGINS=

# === App defaults (safe for local/CI) ===
PROJECT_ID=etsy-ai-hacker
TEST_UID=testuser123
RUN_ID=manual-run
TEST_ALLOW_CLI_CHECKOUT=0

# === Rate Limiting ===
RATE_LIMIT_PER_MIN=12

# === Emulator hints (optional) ===
# FIRESTORE_EMULATOR_HOST=localhost:8080
# FUNCTIONS_EMULATOR_HOST=localhost:5001